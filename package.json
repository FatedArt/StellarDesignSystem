{
  "name": "stellar-design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Stellar Design System – monorepo",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --parallel -r run dev",
    "lint": "pnpm -r run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md,mdx,css}\"",
    "prestorybook": "pnpm tokens:build",
    "storybook": "pnpm --filter @fatedart/storybook run storybook",
    "docs": "pnpm --filter @fatedart/docs run start",
    "tokens:build": "pnpm --filter @fatedart/tokens run build",
    "clean": "pnpm -r run clean"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}

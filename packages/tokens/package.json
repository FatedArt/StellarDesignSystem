{
  "name": "@stellar/tokens",
  "version": "0.0.1",
  "description": "Design tokens for Stellar Design System",
  "main": "dist/tokens.css",
  "files": ["dist"],
  "scripts": {
    "build": "style-dictionary build --config sd.config.mjs",
    "dev": "style-dictionary build --config sd.config.mjs --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0"
  }
}
